import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.benchmark',
  appName: 'benchmark-capacitor-angular',
  webDir: 'dist/benchmark-capacitor-angular/browser',
};

export default config;
